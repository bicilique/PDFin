<?xml version="1.0" encoding="UTF-8"?>

<?import javafx.geometry.Insets?>
<?import javafx.scene.control.*?>
<?import javafx.scene.layout.*?>

<StackPane xmlns="http://javafx.com/javafx"
           xmlns:fx="http://javafx.com/fxml"
           fx:controller="com.pdftoolkit.controllers.SplitControllerRedesigned"
           styleClass="content-root">

    <!-- Main Two-Panel Layout -->
    <HBox styleClass="two-panel-layout" HBox.hgrow="ALWAYS" VBox.vgrow="ALWAYS">
        
        <!-- LEFT PANEL: Range Cards Workspace with FAB -->
        <StackPane styleClass="left-panel" HBox.hgrow="ALWAYS" minWidth="400" prefWidth="500">
            <!-- Base Layer: ScrollPane with Range Cards -->
            <ScrollPane fitToWidth="true" styleClass="content-scroll" 
                       style="-fx-background-color: transparent; -fx-border-width: 0;">
                <VBox spacing="16">
                    <padding>
                        <Insets top="24" right="24" bottom="24" left="24"/>
                    </padding>
                    
                    <!-- Header -->
                    <VBox spacing="8">
                        <Label text="%split.title" styleClass="panel-title"/>
                        <Label fx:id="selectedFileInfoLabel" text="%split.noFileSelected" 
                               styleClass="instruction-text" wrapText="true"/>
                    </VBox>
                    
                    <!-- Range Cards Container -->
                    <VBox fx:id="rangeCardListBox" spacing="12" minHeight="200">
                        <!-- Range cards will be added here dynamically -->
                    </VBox>
                    
                    <!-- Empty State (shown when no ranges) -->
                    <StackPane fx:id="rangeEmptyStatePane" minHeight="300" visible="true">
                        <VBox alignment="CENTER" spacing="16">
                            <Label text="📑" style="-fx-font-size: 64px; -fx-opacity: 0.4;"/>
                            <Label text="%split.noRanges" styleClass="empty-state"/>
                            <Label text="%split.addRangeHint" styleClass="hint-label" wrapText="true" textAlignment="CENTER" maxWidth="300"/>
                        </VBox>
                    </StackPane>
                </VBox>
            </ScrollPane>
            
            <!-- Overlay Layer: FAB positioned at top-right -->
            <VBox alignment="TOP_RIGHT" pickOnBounds="false">
                <padding>
                    <Insets top="24" right="24"/>
                </padding>
                <Button fx:id="addRangeButton" styleClass="fab" onAction="#handleAddRange" disable="true">
                    <graphic>
                        <Label text="+" style="-fx-font-size: 24px; -fx-text-fill: white; -fx-font-weight: 600;"/>
                    </graphic>
                    <tooltip>
                        <Tooltip text="%split.addRange"/>
                    </tooltip>
                </Button>
            </VBox>
        </StackPane>
        
        <!-- RIGHT PANEL: Configuration & Primary CTA -->
        <BorderPane styleClass="right-panel" minWidth="380" prefWidth="420" maxWidth="480">
            <!-- Scrollable Configuration Area -->
            <center>
                <ScrollPane fitToWidth="true" styleClass="content-scroll" 
                           style="-fx-background-color: transparent; -fx-border-width: 0;">
                    <VBox spacing="24">
                        <padding>
                            <Insets top="24" right="24" bottom="16" left="24"/>
                        </padding>
                        
                        <!-- Title -->
                        <VBox spacing="4">
                            <Label text="%split.title" styleClass="panel-title"/>
                            <Label text="%split.subtitle" styleClass="panel-subtitle" wrapText="true"/>
                        </VBox>
                        
                        <Separator/>
                        
                        <!-- Step 1: File Selection -->
                        <VBox spacing="12">
                            <Label text="%split.step1" styleClass="section-label"/>
                            <VBox fx:id="fileDropZone" styleClass="drop-zone" alignment="CENTER" 
                                 spacing="12" minHeight="100">
                                <Label text="📄" style="-fx-font-size: 32px; -fx-opacity: 0.7;"/>
                                <Label text="%split.dropHint" styleClass="drop-hint" wrapText="true" textAlignment="CENTER"/>
                            </VBox>
                            <Button fx:id="selectFileButton" text="%split.selectFileButton" 
                                   styleClass="secondary-button" onAction="#handleSelectFile" maxWidth="Infinity"/>
                        </VBox>
                        
                        <Separator/>
                        
                        <!-- Step 2: Mode Selector -->
                        <VBox spacing="12" fx:id="modeSection" disable="true">
                            <Label text="%split.step2" styleClass="section-label"/>
                            
                            <!-- Segmented Toggle for Split Mode -->
                            <HBox spacing="0" styleClass="segmented-control">
                                <ToggleButton fx:id="splitByRangeToggle" text="%split.splitByRange" 
                                             styleClass="toggle-left" maxWidth="Infinity" HBox.hgrow="ALWAYS"/>
                                <ToggleButton fx:id="extractPagesToggle" text="%split.extractPages" 
                                             styleClass="toggle-right" maxWidth="Infinity" HBox.hgrow="ALWAYS"/>
                            </HBox>
                            
                            <!-- Range Type Selection (Custom / Fixed) -->
                            <VBox spacing="8" fx:id="rangeTypeSection">
                                <Label text="%split.rangeType" styleClass="field-label"/>
                                <HBox spacing="8" styleClass="choice-card-group">
                                    <ToggleButton fx:id="customRangesToggle" text="%split.customRanges" 
                                                 styleClass="choice-card" maxWidth="Infinity" HBox.hgrow="ALWAYS"/>
                                    <ToggleButton fx:id="fixedRangesToggle" text="%split.fixedRanges" 
                                                 styleClass="choice-card" maxWidth="Infinity" HBox.hgrow="ALWAYS"/>
                                </HBox>
                            </VBox>
                        </VBox>
                        
                        <Separator fx:id="rangeEditorSeparator" disable="true"/>
                        
                        <!-- Range Editor (for selected range card) -->
                        <VBox spacing="12" fx:id="rangeEditorSection" disable="true">
                            <Label fx:id="rangeEditorTitle" text="%split.rangeEditor" styleClass="section-label"/>
                            
                            <HBox spacing="12" alignment="CENTER_LEFT">
                                <VBox spacing="6" HBox.hgrow="ALWAYS">
                                    <Label text="%split.from" styleClass="field-label"/>
                                    <Spinner fx:id="fromPageSpinner" editable="true" 
                                            styleClass="range-spinner" maxWidth="Infinity"/>
                                </VBox>
                                <Label text="—" style="-fx-font-size: 20px; -fx-padding: 22 0 0 0; -fx-text-fill: #94a3b8;"/>
                                <VBox spacing="6" HBox.hgrow="ALWAYS">
                                    <Label text="%split.to" styleClass="field-label"/>
                                    <Spinner fx:id="toPageSpinner" editable="true" 
                                            styleClass="range-spinner" maxWidth="Infinity"/>
                                </VBox>
                            </HBox>
                            
                            <!-- Validation message -->
                            <Label fx:id="validationLabel" styleClass="validation-error" 
                                  visible="false" wrapText="true" managed="false"/>
                        </VBox>
                        
                        <Separator/>
                        
                        <!-- Step 3: Output Settings -->
                        <VBox spacing="12" fx:id="outputSection" disable="true">
                            <Label text="%split.step3" styleClass="section-label"/>
                            
                            <VBox spacing="8">
                                <Label text="%split.outputFolder" styleClass="field-label"/>
                                <HBox spacing="8">
                                    <TextField fx:id="outputFolderField" HBox.hgrow="ALWAYS" 
                                              editable="false" styleClass="field-input"
                                              promptText="%split.chooseFolder"/>
                                    <Button fx:id="browseFolderButton" styleClass="icon-button" 
                                           onAction="#handleBrowseOutputFolder">
                                        <graphic>
                                            <Label text="📁" style="-fx-font-size: 18px;"/>
                                        </graphic>
                                        <tooltip>
                                            <Tooltip text="%split.chooseFolder"/>
                                        </tooltip>
                                    </Button>
                                </HBox>
                                <Label fx:id="outputInfoLabel" text="%split.outputInfo" 
                                      styleClass="hint-label" visible="false" wrapText="true"/>
                            </VBox>
                        </VBox>
                    </VBox>
                </ScrollPane>
            </center>
            
            <!-- Bottom: Primary CTA Bar (Sticky) -->
            <bottom>
                <VBox styleClass="cta-bar" spacing="12">
                    <padding>
                        <Insets top="16" right="24" bottom="24" left="24"/>
                    </padding>
                    <Separator/>
                    <Button fx:id="splitButton" text="%split.splitNow" styleClass="primary-button" 
                           defaultButton="true" onAction="#handleSplit" disable="true" maxWidth="Infinity"/>
                    <Button fx:id="backButton" text="%app.back" styleClass="secondary-button" 
                           onAction="#handleBack" maxWidth="Infinity"/>
                </VBox>
            </bottom>
        </BorderPane>
    </HBox>

    <!-- Progress Overlay -->
    <StackPane fx:id="progressOverlay" styleClass="progress-overlay" visible="false" managed="false">
        <VBox alignment="CENTER" spacing="32" styleClass="progress-card" maxWidth="500">
            <VBox alignment="CENTER" spacing="16">
                <Label fx:id="progressTitle" text="%split.processing" styleClass="progress-title"/>
                <ProgressBar fx:id="progressBar" prefWidth="400" styleClass="progress-bar" progress="-1"/>
                <Label fx:id="progressMessage" text="%progress.processing" styleClass="progress-message" wrapText="true" textAlignment="CENTER"/>
            </VBox>
            
            <Button fx:id="cancelProcessButton" text="%app.cancel" styleClass="secondary-button" 
                   onAction="#handleCancelProcess"/>
            
            <!-- Success Pane (Initially Hidden) -->
            <VBox fx:id="successPane" alignment="CENTER" spacing="20" visible="false" managed="false">
                <Label text="✓" styleClass="success-icon"/>
                <Label fx:id="successMessage" text="%split.success" 
                      styleClass="success-message" wrapText="true" textAlignment="CENTER" maxWidth="400"/>
                <HBox spacing="16" alignment="CENTER">
                    <Button fx:id="openFolderButton" text="%split.openFolder" 
                           styleClass="secondary-button" onAction="#handleOpenFolder"/>
                    <Button fx:id="processAnotherButton" text="%split.processAnother" 
                           styleClass="primary-button" onAction="#handleProcessAnother"/>
                </HBox>
            </VBox>
        </VBox>
    </StackPane>

</StackPane>
            <VBox spacing="12" fx:id="rangeEditorSection" disable="true">
                <Label text="%split.rangeEditor" styleClass="section-label"/>
                
                <HBox spacing="12" alignment="CENTER_LEFT">
                    <VBox spacing="4" HBox.hgrow="ALWAYS">
                        <Label text="%split.from" styleClass="field-label"/>
                        <Spinner fx:id="fromPageSpinner" editable="true" prefWidth="100" 
                                styleClass="range-spinner"/>
                    </VBox>
                    <Label text="—" style="-fx-font-size: 18px; -fx-padding: 20 0 0 0;"/>
                    <VBox spacing="4" HBox.hgrow="ALWAYS">
                        <Label text="%split.to" styleClass="field-label"/>
                        <Spinner fx:id="toPageSpinner" editable="true" prefWidth="100" 
                                styleClass="range-spinner"/>
                    </VBox>
                </HBox>
                
                <!-- Validation message -->
                <Label fx:id="validationLabel" styleClass="validation-error" 
                      visible="false" wrapText="true" managed="false"/>
            </VBox>
            
            <Separator/>
            
            <!-- Output Settings -->
            <VBox spacing="12" fx:id="outputSection" disable="true">
                <Label text="%split.step3" styleClass="section-label"/>
                
                <VBox spacing="8">
                    <Label text="%split.outputFolder" styleClass="field-label"/>
                    <HBox spacing="8">
                        <TextField fx:id="outputFolderField" HBox.hgrow="ALWAYS" 
                                  editable="false" styleClass="field-input"
                                  promptText="%split.chooseFolder"/>
                        <Button fx:id="browseFolderButton" styleClass="secondary-button" 
                               onAction="#handleBrowseOutputFolder">
                            <graphic>
                                <Label text="📁" style="-fx-font-size: 16px;"/>
                            </graphic>
                        </Button>
                    </HBox>
                </VBox>
            </VBox>
            
            <!-- Spacer to push button to bottom -->
            <Region VBox.vgrow="ALWAYS"/>
            
            <!-- Primary Action Button -->
            <Button fx:id="splitButton" text="%split.splitNow" styleClass="primary-button" 
                   defaultButton="true" onAction="#handleSplit" disable="true" maxWidth="Infinity"/>
            
            <!-- Back Button -->
            <Button fx:id="backButton" text="%app.back" styleClass="secondary-button" 
                   onAction="#handleBack" maxWidth="Infinity"/>
        </VBox>
    </HBox>

    <!-- Progress Overlay -->
    <StackPane fx:id="progressOverlay" styleClass="progress-overlay" visible="false" managed="false">
        <VBox alignment="CENTER" spacing="32" styleClass="progress-card">
            <VBox alignment="CENTER" spacing="16">
                <Label fx:id="progressTitle" text="%split.processing" styleClass="progress-title"/>
                <ProgressBar fx:id="progressBar" prefWidth="400" styleClass="progress-bar" progress="-1"/>
                <Label fx:id="progressMessage" text="%progress.processing" styleClass="progress-message"/>
            </VBox>
            
            <Button fx:id="cancelProcessButton" text="%app.cancel" styleClass="secondary-button" 
                   onAction="#handleCancelProcess"/>
            
            <!-- Success Pane (Initially Hidden) -->
            <VBox fx:id="successPane" alignment="CENTER" spacing="20" visible="false" managed="false">
                <Label text="✓" styleClass="success-icon"/>
                <Label fx:id="successMessage" text="%split.success" 
                      styleClass="success-message" wrapText="true" textAlignment="CENTER" maxWidth="400"/>
                <HBox spacing="16" alignment="CENTER">
                    <Button fx:id="openFolderButton" text="%split.openFolder" 
                           styleClass="secondary-button" onAction="#handleOpenFolder"/>
                    <Button fx:id="processAnotherButton" text="%split.processAnother" 
                           styleClass="primary-button" onAction="#handleProcessAnother"/>
                </HBox>
            </VBox>
        </VBox>
    </StackPane>

</StackPane>
